# Authentication Configuration
# Copy this file to .env.local for local development
# For production deployment, set these as deployment secrets

# Replit Domains - Comma-separated list of domains where your app will be accessible
# For Replit deployments, use your deployment URL
# Example: myapp-username.replit.app
REPLIT_DOMAINS=your-deployment-domain.replit.app

# Repl ID - Your Replit application's unique identifier
# Get this from your Repl metadata or URL
REPL_ID=your-repl-id-here

# Session Secret - A secure random string for session encryption
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: Use a strong, unique secret for production
SESSION_SECRET=your-super-secret-session-key-here-make-it-long-and-random

# Database Configuration (usually auto-provided by Replit)
DATABASE_URL=postgresql://username:password@host:port/database

# Optional: Custom OIDC issuer URL (defaults to https://replit.com/oidc)
# ISSUER_URL=https://replit.com/oidc